# 更新日志

## 未发布

- 删除公开版误留的官服二维码成绩抓取、转换、保存和上传整条调用链，同步移除 `maimai-update` CLI、AstrBot `mai bind` / `mai update` 直连路由、配置、文档与专属测试；独立落雪 OAuth 和 Diving-Fish 通用 API 鉴权能力保持不变。
- 新增独立落雪 OAuth 绑定 MCP：授权状态、令牌和待确认记录仅保存在权限收紧的本地 SQLite，部署复制与 Docker 构建会排除该私密运行目录；支持绑定状态查询和解绑，不查成绩、不接入 SEGA 官方成绩接口、不包含日服功能。
- 落雪 OAuth 数据库只接受权限为 `0700` 的专用目录，不会改写共享目录权限；过期 state 与待确认 token 会在启动和后续状态操作时清理。
- 落雪 OAuth MCP 只接受公开文档列出的 `maimai_lxns_*` 工具名，不保留额外的无前缀调用入口。
- AstrBot 落雪绑定同时支持群内手工提交授权 code/完整回调 URL，以及回调桥轮询后由原用户在原适配器、原会话中拍一拍当前机器人确认的流程。
- AstrBot 对落雪绑定的敏感命令在解析失败时也只记录工具类别，不记录命令原文、用户标识或参数。
- 新增 AstrBot 插件可复现打包脚本，用固定白名单同时构建并校验两个字节一致的插件 ZIP。
- 补回 B50、群榜和 QQ 身份模块共同依赖的通用 `player_cache` 代码；仓库不包含任何玩家缓存数据，运行目录继续默认忽略。
- 静态资源回归测试改用运行时生成的占位底图和字体替代项，测试不再要求把已移除的通用图片重新提交到公开分支。
- 新增通用落雪 OAuth 回调桥：使用签名 state、强制 Bearer 轮询鉴权、单次原子消费和已消费墓碑，轮询响应不回显 state，并提供不含实例信息的 Nginx 与 systemd 部署样例。
- 重建公开分支资源历史，只保留自定义“雪峰”牌子及其渲染依赖的 20 张图片，其余静态图片改为按需下载且默认不纳入 Git。
- 修复查歌文本和曲目信息图的定数显示，整数定数会保留一位小数，例如 `13.0` 不再显示成 `13`。
- 修复同一歌曲同时具有标准谱面和 DX 谱面时的成绩匹配，避免兼容曲目编号把另一种谱面的成绩错误填入图片，并覆盖显式 DX 编号查询的双谱面展开。
- 修复 NapCat 已经提交图片、但等待消息回执超时时被误判为发送失败的问题；该状态会标记为送达未知，避免错误提示和重复补发。
- 常驻查歌进程会监控落雪曲库、水鱼曲库、别名和定数统计文件；文件被外部刷新后自动失效内存缓存，无需重启进程。
